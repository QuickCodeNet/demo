@model QuickCode.Demo.Portal.Models.PricingEngineModule.CommissionRuleGetByModelIdData
@{
    string title = "GetByModelId".SplitCamelCaseToString();
    TempData["Title"] = title;
}
<div class="container-fluid portal-query-page" IsPageAvailable="true">
    <form method="post" id="formList" data-portal-query="true" class="portal-entity-form portal-query-form" data-list-partial-url="/PricingEngineModuleCommissionRules/Queries/GetByModelIdPartial">
        @Html.AntiForgeryToken()
        <div class="portal-query-shell">
            <header class="portal-query-header">
                <div class="portal-query-header__copy">
                    <p class="portal-query-kicker">Live Report</p>
                    <h2 class="portal-query-title">@title</h2>
                    <p class="portal-query-lead">Set the parameters, run the report, then scan the rows below.</p>
                </div>
                <div class="portal-query-endpoint" title="API endpoint used by this report">
                    <span class="portal-query-endpoint__method">GET</span>
                    <code class="portal-query-endpoint__path">/api/pricing-engine-module/commission-rules/get-by-model-id/{commissionRuleCommissionModelId}?pageNumber={pageNumber}&pageSize={pageSize}</code>
                </div>
            </header>

            <section class="portal-query-workspace" aria-label="Report parameters">
                <div class="portal-query-toolbar">
                    <div class="portal-query-params portal-entity-form">
<form-group>
	<select bc-label="Commission Rule Commission Model ID" class="form-select searchable-select" asp-for="CommissionRuleCommissionModelId" asp-items="@Model.GetComboBoxList("CommissionModels", Model.CommissionRuleCommissionModelId)"  data-placeholder="Search for Commission Rule Commission Model ID..."></select>
</form-group>
                    </div>
                    <div class="portal-query-actions">
                        <button type="submit" class="portal-query-run">
                            <i class="fas fa-play" aria-hidden="true"></i>
                            <span>Run Report</span>
                        </button>
                    </div>
                </div>
            </section>

            <section id="list-region" class="portal-list-region portal-query-results" aria-label="Report results">
                @await Html.PartialAsync("_GetByModelIdTable", Model)
            </section>
        </div>
        <input type="hidden" asp-for="CurrentPage" />
        <input type="hidden" asp-for="PageSize" />
        <input type="hidden" asp-for="HasRun" />
    </form>
</div>
<div class="container-fluid" IsPageAvailable="false">
 @await Html.PartialAsync("_NotAuthorized")
</div>
